const auxillary = props => props.children; // anything within auxillary tags where its called are its children. Not where its defined. Here it is defined.

export default auxillary;

// Here you are not importing React from 'react' as you are not using any JSX.
